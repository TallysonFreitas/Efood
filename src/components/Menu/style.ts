import styled from 'styled-components'

export const Container = styled.div`
  padding: 80px;

  .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    flex-direction: row;
    gap: 48px 80px;
  }
`
